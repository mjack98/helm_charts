apiVersion: v1
kind: Service
metadata:
  name: {{ include "python_task.fullname" . }}
  labels:
    {{- include "python_task.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python_task.selectorLabels" . | nindent 4 }}
